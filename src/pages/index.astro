---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import BlogCard from '../components/BlogCard.astro';
import ScrollProgress from '../components/ScrollProgress.astro';
import ScrollReveal from '../components/ScrollReveal.astro';
import { getCollection } from 'astro:content';
import StructuredData from '../components/StructuredData.astro';

// Base path for GitHub Pages deployment
const basePath = '/astro-blog-2026/';

const siteUrl = Astro.site?.href || 'https://your-domain.com';

const allPosts = await getCollection('posts');
const recentPosts = allPosts
  .sort((a, b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime())
  .slice(0, 6);

const transformedPosts = recentPosts.map(post => ({
  id: post.id,
  title: post.data.title,
  slug: post.slug,
  excerpt: post.data.description || '',
  coverImage: null,
  date: post.data.pubDate,
  category: post.data.tags?.[0] || 'General',
  tags: post.data.tags || [],
  readTime: post.data.readingTime ? `${post.data.readingTime} 分钟` : '5 分钟'
}));

const features = [
  {
    icon: '⚡',
    title: '极速加载',
    description: '使用 Astro 构建，追求最大性能和最小 JavaScript 打包体积。',
    gradient: 'from-amber-400 to-orange-500'
  },
  {
    icon: '🎨',
    title: '精美设计',
    description: '现代化 UI，流畅动画，渐变效果，注重细节。',
    gradient: 'from-pink-400 to-rose-500'
  },
  {
    icon: '📱',
    title: '完全响应式',
    description: '针对从手机到大屏幕的所有设备进行优化。',
    gradient: 'from-cyan-400 to-blue-500'
  }
];
---

<Layout title="Astro 博客 - 首页" description="使用 Astro 构建的现代化博客">
  <StructuredData
    type="WebSite"
    data={{
      url: siteUrl,
      name: 'Astro 博客',
      description: '使用 Astro 5 构建的现代化博客',
      authorName: 'Your Name',
      siteName: 'Astro 博客',
      logo: new URL('/favicon.svg', Astro.site).href,
    }}
  />

  <ScrollProgress />
  <ScrollReveal />

  <Hero />

  <!-- Recent Posts Section -->
  <section class="py-20 sm:py-28 bg-white dark:bg-gray-900 relative overflow-hidden">
    <div class="absolute inset-0 pointer-events-none">
      <div class="absolute top-0 left-1/4 w-64 h-64 bg-primary-100 dark:bg-primary-900/20 rounded-full blur-3xl opacity-50 animate-pulse-slow"></div>
      <div class="absolute bottom-0 right-1/4 w-64 h-64 bg-accent-100 dark:bg-accent-900/20 rounded-full blur-3xl opacity-50 animate-pulse-slow" style="animation-delay: 1.5s;"></div>
    </div>
    
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative">
      <div class="text-center mb-16 animate-scroll-reveal">
        <span class="inline-block px-4 py-1.5 rounded-full text-sm font-medium bg-primary-100 dark:bg-primary-900/30 text-primary-700 dark:text-primary-300 mb-4 animate-fade-in-up">
          博客文章
        </span>
        <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
          最新<span class="gradient-text">文章</span>
        </h2>
        <p class="text-lg text-gray-600 dark:text-gray-400 max-w-2xl mx-auto">
          探索关于 Web 开发和技术的最新思考和教程。
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">
        {transformedPosts.map((post, index) => (
          <div 
            class={`animate-scroll-reveal card-lift ${index % 3 === 0 ? 'animate-stagger-1' : index % 3 === 1 ? 'animate-stagger-2' : 'animate-stagger-3'}`}
          >
            <BlogCard post={post} />
          </div>
        ))}
      </div>

      <div class="text-center mt-12 animate-scroll-reveal">
        <a
          href={`${basePath}blog`}
          class="group inline-flex items-center gap-2 px-8 py-4 rounded-2xl font-semibold text-gray-700 dark:text-gray-300 bg-gray-100 dark:bg-gray-800 hover:bg-gray-200 dark:hover:bg-gray-700 transition-all duration-300 hover:scale-105 hover:shadow-lg magnetic-btn"
        >
          查看全部文章
          <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="py-20 sm:py-28 bg-gray-50 dark:bg-gray-950 relative overflow-hidden">
    <div class="absolute inset-0 bg-[linear-gradient(to_right,#80808005_1px,transparent_1px),linear-gradient(to_bottom,#80808005_1px,transparent_1px)] bg-[size:48px_48px]"></div>
    
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative">
      <div class="text-center mb-16 animate-scroll-reveal">
        <span class="inline-block px-4 py-1.5 rounded-full text-sm font-medium bg-accent-100 dark:bg-accent-900/30 text-accent-700 dark:text-accent-300 mb-4">
          核心特性
        </span>
        <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
          为什么选择<span class="gradient-text">Astro</span>
        </h2>
        <p class="text-lg text-gray-600 dark:text-gray-400 max-w-2xl mx-auto">
          构建现代化网站的最佳选择
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {features.map((feature, index) => (
          <div 
            class={`animate-scroll-reveal feature-card group relative p-8 rounded-2xl bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 hover:border-primary-300 dark:hover:border-primary-700 transition-all duration-500 overflow-hidden ${index === 0 ? 'animate-stagger-1' : index === 1 ? 'animate-stagger-2' : 'animate-stagger-3'}`}
          >
            <div class={`absolute inset-0 bg-gradient-to-br ${feature.gradient} opacity-0 group-hover:opacity-5 transition-opacity duration-500`}></div>
            <div class="relative">
              <div class={`feature-icon w-14 h-14 rounded-2xl bg-gray-100 dark:bg-gray-800 flex items-center justify-center text-2xl mb-6 group-hover:scale-110 group-hover:bg-gradient-to-br group-hover:from-white group-hover:to-gray-100 dark:group-hover:from-gray-800 dark:group-hover:to-gray-900 transition-all duration-300 animate-float`}
                style={`animation-delay: ${index * 0.5}s;`}
              >
                {feature.icon}
              </div>
              <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-3 group-hover:text-primary-600 dark:group-hover:text-primary-400 transition-colors duration-300">
                {feature.title}
              </h3>
              <p class="text-gray-600 dark:text-gray-400 leading-relaxed">
                {feature.description}
              </p>
            </div>
            <div class={`absolute -bottom-2 -right-2 w-20 h-20 bg-gradient-to-br ${feature.gradient} rounded-full opacity-0 group-hover:opacity-20 blur-xl transition-opacity duration-500`}></div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 sm:py-28 bg-gradient-to-br from-primary-600 via-primary-700 to-accent-700 relative overflow-hidden">
    <div class="absolute inset-0 overflow-hidden">
      <div class="absolute -top-1/2 -left-1/4 w-full h-full bg-gradient-to-br from-white/10 to-transparent rounded-full blur-3xl animate-float"></div>
      <div class="absolute -bottom-1/2 -right-1/4 w-full h-full bg-gradient-to-tl from-accent-500/20 to-transparent rounded-full blur-3xl animate-float" style="animation-delay: 2s;"></div>
      <!-- Animated particles -->
      <div class="absolute top-1/4 left-1/4 w-2 h-2 bg-white/30 rounded-full animate-ping"></div>
      <div class="absolute top-3/4 right-1/3 w-3 h-3 bg-white/20 rounded-full animate-ping" style="animation-delay: 1s;"></div>
      <div class="absolute bottom-1/4 left-1/2 w-2 h-2 bg-white/25 rounded-full animate-ping" style="animation-delay: 2s;"></div>
    </div>
    
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center relative">
      <div class="animate-scroll-reveal">
        <span class="inline-block px-4 py-1.5 rounded-full text-sm font-medium bg-white/20 text-white mb-6 backdrop-blur-sm">
          开始探索
        </span>
        <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-white mb-6 animate-fade-in-up">
          准备开始了吗？
        </h2>
        <p class="text-lg sm:text-xl text-white/80 mb-10 max-w-2xl mx-auto animate-fade-in-up" style="animation-delay: 0.2s;">
          探索更多精彩内容，或者联系我一起交流技术。
        </p>
        
        <div class="flex flex-col sm:flex-row items-center justify-center gap-4 animate-fade-in-up" style="animation-delay: 0.4s;">
          <a
            href={`${basePath}blog`}
            class="group inline-flex items-center gap-2 px-8 py-4 rounded-2xl font-semibold bg-white text-primary-700 hover:bg-gray-100 transition-all duration-300 hover:scale-105 shadow-lg hover:shadow-xl magnetic-btn"
          >
            浏览博客
            <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </a>
          
          <a
            href={`${basePath}about`}
            class="group inline-flex items-center gap-2 px-8 py-4 rounded-2xl font-semibold text-white border-2 border-white/30 hover:border-white/60 hover:bg-white/10 transition-all duration-300 hover:scale-105 backdrop-blur-sm magnetic-btn"
          >
            关于我
            <svg class="w-5 h-5 group-hover:rotate-45 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
            </svg>
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>
